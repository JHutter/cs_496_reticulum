<div class="jumbotron">
  <h1>Sign in</h1>
  {{#if msg}}
    <div class="alert alert-danger"><h3>{{msg}}</h3></div>
  {{/if}}
  <p>Please sign-in to your account to view your profile.</p>

  <div id="local" class="">
    <a data-toggle="collapse" href="#local-sign-in" class="btn btn-default btn-md" role="button">I already have an account</a>
    <a data-toggle="collapse" href="#local-reg" class="btn btn-default btn-md" role="button">I need to make an account</a>
  </div>

  <form id="local-sign-in" class="collapse" action="/login" method="post">
    <div>
        <p></p>
        <label>E-mail:</label>
        <input type="text" name="email"/>
    </div>
    <div>
        <label>Password:</label>
        <input type="password" name="password"/>
    </div>
    <div>
        <input type="submit" class="btn btn-primary btn-sm" value="Log In"/>
    </div>
  </form>
  <form id="local-reg" class="collapse" action="/local-reg" method="post">
    <div>
        <p></p>
        <label>E-mail:</label>
        <input type="text" name="email"/>
    </div>
    <div>
        <label>Password:</label>
        <input type="password" name="password"/>
    </div>
    <div>
        <label>Select Region:</label>
        <select id="rid" name="rid">
        {{#each regions}}
             <option value={{regionID}}>{{regionID}} - {{regionName}}</option>
        {{/each}}
        </select>
    </div>
    <div>
        <label>First Name:</label>
        <input type="text" name="fname"/>
    </div>
    <div>
        <label>Last Name:</label>
        <input type="text" name="lname"/>
    </div>
    <div>
        <input type="submit" class="btn btn-primary btn-sm" value="Register"/>
    </div>
  </form>
  <div class="panel-footer">
    <a href="/resetPass"><button id="resetPass" button type="button" class="btn btn-primary">Reset Password</button></a>
  </div>
</div>
